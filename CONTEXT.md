# Folkkit Repository Context

Folkkit is a new independent product and uses this repository as its only primary code workspace.

Read in order:

1. `AGENTS.md`
2. `PROJECT_MEMORY.md`
3. `docs/superpowers/specs/2026-08-31-folkkit-design.md`

For implementation details, load only the relevant inherited documents in `docs/`. The approved Folkkit design overrides conflicting product, visual, privacy, deployment, and naming assumptions inherited from Convert Everything.

Canonical remotes:

- `origin`: `https://github.com/ThisIsPhantom/folkkit.git`
- `upstream`: `https://github.com/MercuriusDream/convert-everything.git`
